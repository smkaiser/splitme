# TODO

- [x] Implement user authentication
- [ ] Enable changing the name of the app for each trip (besides SplitMe). Not sure if this is really needed.
- [ ] Create landing page with login link
- [x] Allow "with the link" users to participate in trips without login
- [ ] If a user is logged in, don't show the "Account" card on the admin page. Show a small icon/name in the top corner of each page that the user can click to manage their account or sign out. If possible, get the avatar from the user's auth provider and display it as the icon.
- [ ] Don't use the word "slug". Trip ID is fine.

